{
    "version": 2,
    "builds": [
      { "src": "back/**", "use": "@now/node" },
      { "src": "front/**", "use": "@now/static" }
    ],
    "routes": [
        { "src": "/api/(.*)", "dest": "/back/server.js/$1"},
        { "src": "/(.*)", "dest": "/front/$1" }
    ]
  }
  